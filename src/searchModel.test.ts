import { describe, expect, it } from 'vitest'
import { filterSearchResults, normalizeSearchText, searchEmptyState, searchMatches } from './searchModel'
describe('Phase-2.4 search model',()=>{it('matches case-insensitively and ignores combining marks without mutating source text',()=>{const source='ٱلْحَمْدُ لِلَّهِ';expect(searchMatches(source,'الحمد')).toBe(true);expect(source).toBe('ٱلْحَمْدُ لِلَّهِ');expect(normalizeSearchText('Allah')).toBe(normalizeSearchText('allah'))});it('filters selected legacy and Phase 3E learning content types',()=>{const results=[{type:'quran' as const,id:'q'},{type:'names' as const,id:'n'},{type:'duas' as const,id:'d'},{type:'azkar' as const,id:'a'},{type:'learning' as const,id:'l'}];expect(filterSearchResults(results,'quran').map(x=>x.id)).toEqual(['q']);expect(filterSearchResults(results,'learning').map(x=>x.id)).toEqual(['l']);expect(filterSearchResults(results,'all').map(x=>x.id)).toEqual(['q','n','d','a','l'])});it('distinguishes idle, results, and no-result search states',()=>{expect(searchEmptyState('   ',0)).toBe('idle');expect(searchEmptyState('Allah',2)).toBe('results');expect(searchEmptyState('missing',0)).toBe('no-results')})})