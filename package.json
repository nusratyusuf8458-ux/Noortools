{
  "name": "noortools",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "pretest": "node scripts/fetch-tanzil-quran.mjs",
    "typecheck": "tsc -b --pretty false",
    "build": "node scripts/fetch-tanzil-quran.mjs && npm run typecheck && vite build",
    "preview": "vite preview",
    "test": "vitest run --environment jsdom",
    "test:watch": "vitest --environment jsdom",
    "lint": "tsc --noEmit --pretty false"
  },
  "dependencies": {
    "@photostructure/tz-lookup": "^11.6.1",
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest",
    "vitest": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "jsdom": "latest"
  }
}
